export const environment = {
  production: true,
  // Safe placeholders for GitHub. Inject real values during deployment or replace locally.
  supabaseUrl: 'https://your-project.supabase.co',
  supabaseAnonKey: 'your-supabase-anon-key',
};
